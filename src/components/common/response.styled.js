import styled from "styled-components";

const StyledResponseEmpty = styled.p`
  padding: 32px 16px;
  text-align: center;
  font-weight: 700;
  color: var(--secondary);
`

export {
  StyledResponseEmpty as ResponseEmpty
}